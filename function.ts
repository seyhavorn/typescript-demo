function add(num1: number, num2: number): number {
	return num1 + num2;
}

let myResults = add(10, 20);
console.log(`My result is : ${ myResults }`);

const sumData = (num1: number, num2: number): number => num1 + num2;
